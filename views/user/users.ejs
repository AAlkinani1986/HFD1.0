<%- include('../partials/header');%> 

<main>
    <div class="container userTable">
      <%- include('../partials/messages');%>

        <table class="striped green darken-4 centered highlight userlist responsive-table col s12 m12 l12" style="color: white;">
          <thead style="font-size: large; font-weight: bold;">
            <tr>
              <th >Username</th>
              <th >E-Mail</th>
              <th >Password</th>
              <th >Occupation</th>
              <th >Verified</th>
              <th >Actions</th>
            </tr>
          </thead>
          <tbody>

            <% users.forEach(function(user) { %>
              <tr>
                <td>
                  <%=user.username%>
                </td>
                <td>
                  <%=user.email%>
                </td>
                <td  style="font-size: small;">
                  <%=user.password%>
                </td>
                <td>
                    <%=user.occupation%>
                  </td>
                <td>
                  <%=user.verified%>
                </td>
                <td>

                  <% if(user.verificationToken) {%>
                    <a href="/user/verify/<%=user._id%>/<%=user.verificationToken%>"class=" btn-floating btn_check"
                      title="Verify user"><i class="material-icons">check_circle</i></a>
                    <%}%>

                      <% if(user.resetToken) {%>
                        <a href="/user/changePassword/<%=user._id%>/<%=user.resetToken%>"class=" btn-floating btn_undo"
                          title="Reset password"><i class="material-icons">undo</i></a>
                        <%}%>

                       
                          <a href="/user/delete/<%=user._id%>" class=" btn-floating btn_delete"
                            title="Delete user"><i class="material-icons"> delete_forever</i></a>

                </td>
              </tr>
              <%});%>
          </tbody>
        </table>

    </div>
  </main>